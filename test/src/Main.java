import java.util.Scanner;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        // Press Alt+Enter with your caret at the highlighted text to see how
        // IntelliJ IDEA suggests fixing it.

                String str= "abvfdjfj";
                Scanner sc = new Scanner(System.in);
                String strinput= sc.next();
        System.out.println(str);
        char[] input= str.toCharArray();
        for(int i=0; i< input.length; i++){
            System.out.println(str);
            if(input[i]== strinput)
            {
                System.out.println("visible");
            }

        }

            }
        }

@dataprovider('wef',123)
        scenario outline :
        get ""

Examples:
||


// new app
1. version of the build 8.9.10 -> stable build verification
2. name of app
3. package name of app is verfified
4. the accessability -> precondition (permission)
if permission not enbaled, pop up saying allow
5. notification -> if allowed, drop down on home page
6. launch app open
7. if we go back button, the app should not lauch
8. home buuton, should go back to home page

